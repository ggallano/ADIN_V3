<UserControl x:Class="ADIN.WPF.View.StatusStripView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ADIN.WPF.View"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5 0 0 0">
            <!--<TextBlock Text="Script: " VerticalAlignment="Center"/>-->
            <ComboBox ItemsSource="{Binding Scripts}" Width="180" SelectedItem="{Binding SelectedScript}" Style="{StaticResource ComboBoxFlatStyle}" Height="25">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <telerik:RadButton Content="Apply" Margin="10 0 0 0" Style="{StaticResource FaultDetectorOkButtonStyle}" Command="{Binding ScriptApplyCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
